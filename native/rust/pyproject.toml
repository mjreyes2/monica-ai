[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "monica_audio_accel"
version = "1.0.0"
description = "Rust high-performance audio pipeline for Monica AI"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
